import React from 'react';
import NavBar from "../NavBar/NavBar";

const SideBar: React.FC = () => {
    return (
        <div>
            <NavBar/>
        </div>
    );
};

export default SideBar;